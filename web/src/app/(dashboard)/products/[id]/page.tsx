// 🚀 FITUR: Master Data Produk - Dinamik route untuk edit/detail produk
export default function ProductDetailPage({ params }: { params: { id: string } }) {
  return <div>Product Detail {params.id}</div>;
}